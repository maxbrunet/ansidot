---
- hosts: all
  tasks:
    - name: Install configurations and plugins
      include_role:
        name: .
      with_items: '{{ apps }}'
      loop_control:
        loop_var: app
      tags:
        - packages
        - git
        - directories
        - dotfiles
